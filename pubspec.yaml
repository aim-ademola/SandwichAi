name: sandwich_ai
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+24

environment:
  sdk: ^3.9.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  go_router: ^17.0.0
  flutter_bloc: ^9.1.1
  flutter_svg: ^2.2.2
  intl: ^0.20.2
  image_picker: ^1.2.1
  path_provider: ^2.1.5
  file_picker: ^10.3.7
  record: ^6.1.2
  iconsax: ^0.0.8
  connectivity_plus: ^7.0.0
  dio: ^5.9.0
 
  equatable: ^2.0.7
  shimmer: ^3.0.0
  internet_connection_checker_plus: ^2.9.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_launcher_icons: ^0.14.4
  fl_chart: ^1.1.1
  animate_to: ^0.1.0
  showcaseview: ^5.0.1
  timezone: ^0.10.1
  flutter_local_notifications: ^19.5.0
  shared_preferences: ^2.5.4
  pdf: ^3.11.3
  printing: ^5.14.2
  open_file: ^3.5.11
  device_info_plus: ^12.3.0
  permission_handler: ^12.0.1
  flutter_secure_storage: ^10.0.0
  share_plus: ^12.0.1
  google_mlkit_text_recognition: ^0.15.1
  mobile_scanner: ^7.1.4
  flutter_blue_plus: ^2.1.1
  usb_serial: ^0.5.2
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/img/android.png" 
  
  # iOS-specific configuration
  image_path_ios: "assets/img/ios-logo.png"  
  remove_alpha_ios: true
  # Android adaptive icon (optional)
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/img/android.png"

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/svg/
    - assets/img/
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
  - family: Work_Sans
    fonts:
      - asset: assets/font/WorkSans-Thin.ttf
        weight: 100
      - asset: assets/font/WorkSans-ThinItalic.ttf
        weight: 100
        style: italic

      - asset: assets/font/WorkSans-ExtraLight.ttf
        weight: 200
      - asset: assets/font/WorkSans-ExtraLightItalic.ttf
        weight: 200
        style: italic

      - asset: assets/font/WorkSans-Light.ttf
        weight: 300
      - asset: assets/font/WorkSans-LightItalic.ttf
        weight: 300
        style: italic

      - asset: assets/font/WorkSans-Regular.ttf
        weight: 400
      - asset: assets/font/WorkSans-Italic.ttf
        weight: 400
        style: italic

      - asset: assets/font/WorkSans-Medium.ttf
        weight: 500
      - asset: assets/font/WorkSans-MediumItalic.ttf
        weight: 500
        style: italic

      - asset: assets/font/WorkSans-SemiBold.ttf
        weight: 600
      - asset: assets/font/WorkSans-SemiBoldItalic.ttf
        weight: 600
        style: italic

      - asset: assets/font/WorkSans-Bold.ttf
        weight: 700
      - asset: assets/font/WorkSans-BoldItalic.ttf
        weight: 700
        style: italic

      - asset: assets/font/WorkSans-ExtraBold.ttf
        weight: 800
      - asset: assets/font/WorkSans-ExtraBoldItalic.ttf
        weight: 800
        style: italic

      - asset: assets/font/WorkSans-Black.ttf
        weight: 900
      - asset: assets/font/WorkSans-BlackItalic.ttf
        weight: 900
        style: italic

  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
